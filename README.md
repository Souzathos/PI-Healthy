# PI-Healthy

CREATE DATABASE healthly; 
USE healthly;

CREATE TABLE usuarios ( id INT AUTO_INCREMENT PRIMARY KEY, nome VARCHAR(255) NOT NULL, email VARCHAR(255) NOT NULL UNIQUE, senha_hash VARCHAR(255) NOT NULL );
